import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { PaddingSize } from '../../subatoms';
import { ProgressCounter, IProgressCounterProps } from '.';
import { KibaIcon } from '../../subatoms';

<Meta
  component={ProgressCounter}
  title={'Molecules/ProgressCounter'}
/>

# ProgressCounter

<br/>

export const Template = (args) => <ProgressCounter {...args} />;

<Story name='Default' args={{stepCount: 5, selectedStepIndex: 2}}>
  {Template.bind({})}
</Story>

<ArgsTable story={'Default'} />

<!-- TODO(krish): Document the theming -->
