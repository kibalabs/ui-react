import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { ProgressCounter, IProgressCounterProps } from '.';

<Meta
  component={ProgressCounter}
  title={'Molecules/ProgressCounter'}
/>

# ProgressCounter

<br/>

export const Template = (args) => (
  <ProgressCounter {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{stepCount: 5, selectedStepIndex: 2}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Too many'>
    <ProgressCounter stepCount={200} selectedStepIndex={5} />
  </Story>
</Canvas>

### Theming

TODO
