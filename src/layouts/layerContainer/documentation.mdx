import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# LayerContainer

**LayerContainer** is a `layout` component for stacking layers. Use `LayerContainer.Layer` to control positioning and alignment of each layer.

<Canvas of={Stories.Default} />

## Examples

### Centered Layer

<Canvas of={Stories.Centered} />

### Custom Alignment

<Canvas of={Stories.CustomAlignment} />

### Static Layer

<Canvas of={Stories.StaticLayer} />

## Theming

This is a structural component without visual theming.
