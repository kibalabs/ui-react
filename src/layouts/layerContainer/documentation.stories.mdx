import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { LayerContainer, ILayerContainerProps } from '.';
import { Direction, Alignment } from '../../model';
import { KibaIcon, Box } from '../../particles';
import { BackgroundView } from '../../wrappers';

<Meta
  component={LayerContainer}
  title={'Layouts/LayerContainer'}
/>

# LayerContainer

<br/>

export const Template = (args) => (
  <Box height='30px' width='30px'>
    <LayerContainer {...args}>
      <BackgroundView color='lightblue'>
        <Box isFullHeight={true} isFullWidth={true} />
      </BackgroundView>
      <KibaIcon iconId={'ion-airplane'} />
      <LayerContainer.Layer isFullHeight={false} isFullWidth={false} alignmentVertical={Alignment.Center} alignmentHorizontal={Alignment.Center}>
        <KibaIcon iconId={'ion-american-football'} />
      </LayerContainer.Layer>
    </LayerContainer>
  </Box>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Layers on top of each other'>
    <Box height='100px' width='100px'>
      <LayerContainer>
        <BackgroundView color='lightblue'>
          <Box height='100px' width='100px' />
        </BackgroundView>
        <BackgroundView color='limegreen'>
          <Box height='50px' width='50px'/>
        </BackgroundView>
      </LayerContainer>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Layers on top of each other and centered'>
    <Box height='100px' width='100px'>
      <LayerContainer>
        <BackgroundView color='lightblue'>
          <Box height='100px' width='100px' />
        </BackgroundView>
        <LayerContainer.Layer isFullHeight={false} isFullWidth={false} alignmentVertical={Alignment.Center} alignmentHorizontal={Alignment.Center}>
          <BackgroundView color='limegreen'>
            <Box height='50px' width='50px'/>
          </BackgroundView>
        </LayerContainer.Layer>
      </LayerContainer>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Layers on top of each other with custom alignment'>
    <Box height='100px' width='100px'>
      <LayerContainer>
        <BackgroundView color='lightblue'>
          <Box height='100px' width='100px' />
        </BackgroundView>
        <LayerContainer.Layer isFullHeight={false} isFullWidth={false} alignmentVertical={Alignment.Start} alignmentHorizontal={Alignment.End}>
          <BackgroundView color='limegreen'>
            <Box height='50px' width='50px'/>
          </BackgroundView>
        </LayerContainer.Layer>
      </LayerContainer>
    </Box>
  </Story>
</Canvas>

TODO

### Theming

TODO
