import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { LayerContainer, ILayerContainerProps } from '.';
import { Direction, Alignment } from '../../model';
import { KibaIcon, Box } from '../../particles';
import { BackgroundView } from '../../wrappers';

<Meta
  component={LayerContainer}
  title={'Layouts/LayerContainer'}
/>

# LayerContainer

<br/>

export const Template = (args) => (
  <LayerContainer {...args}>
    <LayerContainer.Layer>
      <KibaIcon iconId={'ion-airplane'} />
      <KibaIcon iconId={'ion-american-football'} />
    </LayerContainer.Layer>
  </LayerContainer>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{childSize: 3}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Icon on top of Box'>
    <LayerContainer childSize={6}>
      <LayerContainer.Layer key={1}>
        <Box variant='card'/>
      </LayerContainer.Layer>
      <LayerContainer.Layer key={2} isFullWidth={false} alignmentVertical={Alignment.Center} alignmentHorizontal={Alignment.Center}>
        <KibaIcon iconId='ion-home' />
      </LayerContainer.Layer>
    </LayerContainer>
  </Story>
</Canvas>

<Canvas>
  <Story name='Layers of Box - 1'>
    <LayerContainer childSize={6}>
      <LayerContainer.Layer key={1}>
        <BackgroundView color='lightblue'>
          <Box variant='bordered' height='100px'/>
        </BackgroundView>
      </LayerContainer.Layer>
      <LayerContainer.Layer key={2} isFullWidth={false} alignmentVertical={Alignment.Center} alignmentHorizontal={Alignment.Center}>
        <BackgroundView color='limegreen'>
          <Box variant='card' height='50px' width='50px'/>
        </BackgroundView>
      </LayerContainer.Layer>
    </LayerContainer>
  </Story>
</Canvas>

<Canvas>
  <Story name='Layers of Box - 2'>
    <LayerContainer childSize={6}>
      <LayerContainer.Layer key={1}>
        <BackgroundView color='lightblue'>
          <Box variant='bordered' height='100px'/>
        </BackgroundView>
      </LayerContainer.Layer>
      <LayerContainer.Layer key={2} isFullWidth={false} alignmentVertical={Alignment.Start} alignmentHorizontal={Alignment.End}>
        <BackgroundView color='limegreen'>
          <Box variant='card' height='50px' width='50px'/>
        </BackgroundView>
      </LayerContainer.Layer>
    </LayerContainer>
  </Story>
</Canvas>

TODO

### Theming

TODO
