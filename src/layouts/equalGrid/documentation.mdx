import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# EqualGrid

**EqualGrid** is a `layout` component for equal-sized grid items. All children take up the same width based on `childSize`.

<Canvas of={Stories.Default} />

<Controls of={Stories.Default} />

## Examples

### Two Per Row

<Canvas of={Stories.TwoPerRow} />

### Four Per Row

<Canvas of={Stories.FourPerRow} />

### With Gutters

<Canvas of={Stories.WithGutters} />

## Theming

This is a structural component without visual theming.
