import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Box } from '../../particles/box';
import { MarkdownText, IMarkdownTextProps } from '.';

<Meta
  component={MarkdownText}
  title={'Molecules/MarkdownText'}
/>

# MarkdownText

<br/>

export const Template = (args) => (
  <MarkdownText {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{source: 'this is an **example**'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Simple'>
    <MarkdownText source={'this is an **example**'} />
  </Story>
</Canvas>

<Canvas>
  <Story name='very long'>
    <Box width='200px'>
      <MarkdownText source={'this is an **example with very long text** so we can see what things might look like if they get split on *multiple lines*, you know.'} />
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='With newlines'>
    <MarkdownText source={'This should be rendered as **a single tag with BRs** in between\n\nIt should not be two separate P tags\nokay?'} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Link'>
    <MarkdownText source={'Made by [Kiba Labs](https://www.kibalabs.com)'} />
  </Story>
</Canvas>

### Theming

TODO
