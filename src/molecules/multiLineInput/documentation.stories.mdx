import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { MultiLineInput, IMultiLineInputProps } from '.';

<Meta
  component={MultiLineInput}
  title={'Molecules/MultiLineInput'}
/>

# MultiLineInput

<br/>

export const Template = (args) => (
  <MultiLineInput {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{placeholderText: 'What do you say?'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples


<Canvas>
  <Story name='Stateful Example'>
    {() => {
      const [value, setValue] = React.useState('');
      return (
        <MultiLineInput value={value} onValueChanged={setValue} />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Success Message'>
    <MultiLineInput inputWrapperVariant='success' messageText='Success message' />
  </Story>
</Canvas>

<Canvas>
  <Story name='Error Message'>
    <MultiLineInput inputWrapperVariant='error' messageText='Error message' />
  </Story>
</Canvas>

<Canvas>
  <Story name='Disabled'>
    <MultiLineInput isEnabled={false} />
  </Story>
</Canvas>

### Theming

TODO
