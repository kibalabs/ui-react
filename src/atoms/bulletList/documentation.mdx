import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# BulletList

**BulletList** is an `atom` component that wraps BulletText items in a semantic unordered list.

<Canvas of={Stories.Default} />

<Controls of={Stories.Default} />

## Examples

### Nested List

<Canvas of={Stories.NestedList} />

### Long Content

<Canvas of={Stories.LongContent} />

## Theming

### CSS Class: `.KibaBulletList`

### Customizing with CSS

```css
@layer kiba-theme {
  .KibaBulletList {
    list-style-type: disc;
    padding-left: var(--kiba-padding);
    margin: 0;
  }
}
```
