import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { LinePager, ILinePagerProps } from '.';
import { PaddingSize, KibaIcon } from '../../particles';

<Meta
  component={LinePager}
  title={'Atoms/LinePager'}
/>

# LinePager

<br/>

export const Template = (args) => (
  <LinePager {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{pageCount: 5, activePageIndex: 1}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Simple'>
    <LinePager variant='default' pageCount={5} activePageIndex={1} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Many'>
    <LinePager variant='default' pageCount={500} activePageIndex={1} />
  </Story>
</Canvas>

### Theming

TODO
