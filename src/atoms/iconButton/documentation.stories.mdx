import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { IconButton, IIconButtonProps } from '.';
import { PaddingSize, KibaIcon } from '../../particles';

<Meta
  component={IconButton}
  title={'Atoms/IconButton'}
/>

# IconButton

<br/>

export const Template = (args) => (
  <IconButton {...args} icon={<KibaIcon iconId='ion-bulb' />}/>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{icon: <KibaIcon iconId='ion-bulb' />}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Primary'>
    <IconButton variant='primary' icon={<KibaIcon iconId='ion-bulb' />} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Secondary'>
    <IconButton variant='secondary' icon={<KibaIcon iconId='ion-bulb' />} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Disabled'>
    <IconButton variant='primary' isEnabled={false} icon={<KibaIcon iconId='ion-bulb' />} />
  </Story>
</Canvas>

### Theming

TODO
