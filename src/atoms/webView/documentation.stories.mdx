import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { WebView, IWebViewProps } from '.';

<Meta
  component={WebView}
  title={'Atoms/WebView'}
/>

# WebView

<br/>

export const Template = (args) => (
  <div style={{height: '300px'}}>
    <WebView {...args} />
  </div>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{url: 'https://www.kibalabs.com'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='With Error'>
    <div style={{height: '300px'}}>
      <WebView url={'https://www.google.com'} />
    </div>
  </Story>
</Canvas>

<Canvas>
  <Story name='With Aspect Ratio'>
    <WebView url={'https://www.youtube.com/embed/_tUCouKc4Ek'} aspectRatio={0.5625} />
  </Story>
</Canvas>

NOTE(krish): the error detection isn't working yet.

### Theming

TODO
