import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Switch, ISwitchProps } from '.';

<Meta
  component={Switch}
  title={'Atoms/Switch'}
/>

# Switch

<br/>

export const Template = (args) => (
  <Switch {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default'>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story='Default' />

# Example

<Canvas>
  <Story name='stateful'>
    {() => {
      const [isChecked, setIsChecked] = React.useState(false);
      return <Switch isChecked={isChecked} onToggled={() => setIsChecked(!isChecked)}/>;
    }}
  </Story>
</Canvas>
