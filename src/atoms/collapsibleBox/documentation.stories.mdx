import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { CollapsibleBox, ICollapsibleBoxProps, StatefulCollapsibleBox } from '.';

<Meta
  component={CollapsibleBox}
  title={'Atoms/CollapsibleBox'}
/>

# CollapsibleBox

<br/>

export const Template = (args) => (
  <CollapsibleBox {...args}>
    <span>This is the content inside the box</span>
  </CollapsibleBox>
);

<Canvas withToolbar={true}>
  <Story name='Default' args={{ headerView: <div style={{height: '50px', backgroundColor: 'yellow', width: '100%'}}>My Cool Box</div>, onCollapseToggled: () => {}}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Stateful'>
    <StatefulCollapsibleBox headerView={<div style={{height: '50px', backgroundColor: 'yellow', width: '100%'}}>Stateful CollapsibleBox</div>}>
      <span> This is the content inside the box </span>
    </StatefulCollapsibleBox>
  </Story>
</Canvas>

<Canvas>
  <Story name='Stateful unpadded'>
    <StatefulCollapsibleBox variant='unpaddedHeader' shouldHideIndicator={true} headerView={<div style={{height: '50px', backgroundColor: 'yellow', width: '100%'}}>Stateful CollapsibleBox (no padding)</div>}>
      <span> This is the content inside the box </span>
    </StatefulCollapsibleBox>
  </Story>
</Canvas>


<Canvas>
  <Story name='Stateful Collapsed'>
    <StatefulCollapsibleBox headerView={<div style={{height: '50px', backgroundColor: 'yellow', width: '100%'}}>Stateful CollapsibleBox (collapsed initially)</div>} isCollapsedInitially={true}>
      <span> This is the content inside the box </span>
    </StatefulCollapsibleBox>
  </Story>
</Canvas>

### Theming

TODO
