import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# ResponsiveTextAlignmentView

**ResponsiveTextAlignmentView** is a `wrapper` component that aligns text based on screen size. Adapts text alignment responsively.

<Canvas of={Stories.Default} />

<Controls of={Stories.Default} />

## Examples

### Responsive Alignment

<Canvas of={Stories.ResponsiveAlignment} />

## Theming

This is a structural component without visual theming.
