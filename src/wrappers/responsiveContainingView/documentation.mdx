import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# ResponsiveContainingView

**ResponsiveContainingView** is a `wrapper` component that limits width based on screen size. Adapts container constraints responsively.

<Canvas of={Stories.Default} />

<Controls of={Stories.Default} />

## Examples

### Responsive Sizes

<Canvas of={Stories.ResponsiveSizes} />

## Theming

This is a structural component without visual theming.
