import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './documentation.stories';

<Meta of={Stories} />

# BackgroundView

**BackgroundView** is a `wrapper` component that applies a background color. Wraps children with a background styling.

<Canvas of={Stories.Default} />

<Controls of={Stories.Default} />

## Examples

### Primary Color

<Canvas of={Stories.PrimaryColor} />

### Light Gradient

<Canvas of={Stories.LightGradient} />

## Theming

This is a structural component without visual theming.
