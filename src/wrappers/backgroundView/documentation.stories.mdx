import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { Box, Text } from '../../subatoms';
import { BackgroundView, IBackgroundViewProps } from '.';

<Meta
  component={BackgroundView}
  title={'Wrappers/BackgroundView'}
/>

# BackgroundView

<br/>

export const Template = (args) => (
  <BackgroundView {...args}>
    <Box variant='card'>
      <Text>I'm wrapped</Text>
    </Box>
  </BackgroundView>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{color: 'lightblue'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

TODO

### Theming

TODO
