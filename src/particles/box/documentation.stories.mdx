import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { Box, IBoxProps } from '.';
import { BoxThemeExample } from './themeExample';
import { PaddingSize, KibaIcon } from '../../particles';

<Meta
  component={Box}
  title={'Particles/Box'}
/>

# Box

<br/>

export const Template = (args) => (
  <Box {...args}>
    <p>I'm in a box</p>
  </Box>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Card'>
    <Box variant='card'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Bordered'>
    <Box variant='bordered'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Padded'>
    <Box variant='padded'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Scrollable Horizontally'>
    <Box width={'100px'} isScrollableHorizontally={true} isFullHeight={true}>
      <div style={{backgroundColor: '#dddddd', width: '1000px', height: '50px'}} />
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Scrollable Vertically'>
    <Box height={'100px'} isScrollableVertically={true} isFullWidth={false}>
      <div style={{backgroundColor: '#dddddd', height: '1000px', width: '50px'}} />
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Empty Card with Tooltip'>
    <Box variant='card' title='Tooltip on the Box' isFullWidth={false} />
  </Story>
</Canvas>

### Theming

**Box** is a type of `particle`. Boxes provide a basic building block for structuring UIs.
The theming structure of a **Box** is as follows:
```ts
interface IBoxTheme extends CssTheme, ThemeType {
  'background-color': string;
  'border-radius': string;
  'border-color': string;
  'border-width': string;
  'border-style': string;
  'padding': string;
  'box-shadow': string;
  'margin': string;
  'outline-style': string;
  'outline-color': string;
  'outline-width': string;
  'outline-offset': string;
}
```

#### Example

<Canvas withSource={'none'}>
  <Story name='comTestBlock'>
    <BoxThemeExample />
  </Story>
</Canvas>
