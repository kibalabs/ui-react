import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Box, IBoxProps } from '.';
import { BoxThemeExample } from './themeExample';
import { PaddingSize, KibaIcon, Image, Text } from '../../particles';
import { LayerContainer, Stack } from '../../layouts';
import { Alignment } from '../../model';

<Meta
  component={Box}
  title={'Particles/Box'}
/>

# Box

<br/>

export const Template = (args) => (
  <Box {...args}>
    <p>I'm in a box</p>
  </Box>
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Card'>
    <Box variant='card'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Bordered'>
    <Box variant='bordered'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Padded'>
    <Box variant='padded'>
      <p>I'm in a box</p>
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Scrollable Horizontally'>
    <Box width={'100px'} isScrollableHorizontally={true} isFullHeight={true}>
      <div style={{backgroundColor: '#dddddd', width: '1000px', height: '50px'}} />
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Scrollable Vertically'>
    <Box height={'100px'} isScrollableVertically={true} isFullWidth={false}>
      <div style={{backgroundColor: '#dddddd', height: '1000px', width: '50px'}} />
    </Box>
  </Story>
</Canvas>

<Canvas>
  <Story name='Empty Card with Tooltip'>
    <Box variant='card' title='Tooltip on the Box' isFullWidth={false} />
  </Story>
</Canvas>

<Canvas>
  <Story name='Clipped Content'>
    <Box variant='rounded-borderColored' shouldClipContent={true} width='200px' height='200px'>
      <LayerContainer>
        <Box isFullWidth={true} isFullHeight={true}>
          <Image fitType='cover' isFullHeight={true} isFullWidth={true} source='https://unsplash.com/photos/gsf4OfORp5c/download?force=true&w=640'/>
        </Box>
        <LayerContainer.Layer isFullWidth={true} isFullHeight={true}>
          <Box isFullHeight={true}>
            <Stack isFullHeight={true} isFullWidth={true}>
              <Stack.Item growthFactor={1} shrinkFactor={1} />
              <Box variant='padded-overlay'>
                <Text variant='paragraph-imageCaption' alignment='center'>Hot air balloon</Text>
                <br />
              </Box>
            </Stack>
          </Box>
        </LayerContainer.Layer>
  </LayerContainer>
    </Box>
  </Story>
</Canvas>

### Theming

**Box** is a type of `particle`. Boxes provide a basic building block for structuring UIs.
The theming structure of a **Box** is as follows:
```ts
interface IBoxTheme extends CssTheme, ThemeType {
  'background-color': string;
  'border-radius': string;
  'border-color': string;
  'border-width': string;
  'border-style': string;
  'padding': string;
  'box-shadow': string;
  'margin': string;
  'outline-style': string;
  'outline-color': string;
  'outline-width': string;
  'outline-offset': string;
}
```

#### Example

<Canvas withSource={'none'}>
  <Story name='comTestBlock'>
    <BoxThemeExample />
  </Story>
</Canvas>
