import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Portal, IPortalProps } from '.';

<Meta
  component={Portal}
  title={'Particles/Portal'}
/>

# Portal

<br/>

export const Template = (args) => (
  <Portal {...args} />
);

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Placed at bottom-right'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='bottom-right'>
            <Box width='200px'>
              <Text>I'm anchored at the bottom-right</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Placed at bottom-left'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='bottom-left'>
            <Box width='200px'>
              <Text>I'm anchored at the bottom-left</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Placed at bottom-center'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='bottom-center'>
            <Box width='200px'>
              <Text>I'm anchored at the bottom-center</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Placed at top-right'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='top-right'>
            <Box width='200px'>
              <Text>I'm anchored at the top-right</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Placed at top-left'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='top-left'>
            <Box width='200px'>
              <Text>I'm anchored at the top-left</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name='Placed at top-center'>
    {() => {
      const ref = React.createRef();
      return (
        <React.Fragment>
          <button ref={ref}>Open Portal</button>
          <Portal anchorElement={ref} placement='top-center'>
            <Box width='200px'>
              <Text>I'm anchored at the top-center</Text>
            </Box>
          </Portal>
        </React.Fragment>
      );
    }}
  </Story>
</Canvas>

### Theming

TODO
