import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import { RecursivePartial } from '@kibalabs/core';

import { ThemeType, IBoxTheme, ITextTheme } from '.';

<Meta title={'Introduction/Welcome'} />

# Introduction

👋 Welcome to Kiba Labs' UI-React (yes, we need a better name 🙄)

This is a React component library designed to explicitly separate that **theming**, **layout** and (wip) **structure** of your applications.
